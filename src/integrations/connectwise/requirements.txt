# AWS Pandas layer provides: pandas, numpy, pyarrow, boto3, botocore,
# requests, python-dateutil, pytz, s3transfer, urllib3, jmespath
# Only include application-specific dependencies not provided by AWS layer
# Note: Using config_simple.py eliminates need for pydantic dependencies